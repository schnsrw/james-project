# JVM sizing for approximately 20k users with active IMAP/SMTP workloads.
JAVA_TOOL_OPTIONS=-Xms4g -Xmx4g
# Automatically create a default domain on first start; additional domains can be
# managed with the CLI as described in the deployment guide.
DOMAIN=example.com
# Uncomment the line below (and remove the previous JAVA_TOOL_OPTIONS definition)
# to enable Glowroot APM instrumentation alongside the heap sizing above.
#JAVA_TOOL_OPTIONS=-javaagent:/root/glowroot.jar -Xms4g -Xmx4g
